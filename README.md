# What Da Shop (Work in Progress)

What Da Shop API is a sample project showcasing GraphQL with MongoDB.

## Installation

To install What Da Shop API and its dependencies, run:

```bash
npm install
```

## Start

To start API, run:

```bash
npm run start
```
